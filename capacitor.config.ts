import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.hightopgames.conezone',
  appName: 'Skate Burger',
  webDir: 'dist',
};

export default config;
